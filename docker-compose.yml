services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: quant
      POSTGRES_PASSWORD: quant
      POSTGRES_DB: quant_picker
    ports:
      - "5432:5432"
    volumes:
      - quant_picker_pg:/var/lib/postgresql/data

volumes:
  quant_picker_pg:
